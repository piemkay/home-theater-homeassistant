# The engine core and the Jellyfin client are unit-tested without Home
# Assistant, so this is deliberately small.
pytest==9.1.1
pytest-asyncio==1.3.0
PyYAML==6.0.2
aiohttp==3.13.2
voluptuous==0.15.2
ruff==0.14.3
